// Test setup for Vitest
// Add any global test setup here (mocks, global variables, etc.)

// Keep this file present for Vitest's `setupFiles` option. If you want
// DOM matchers (e.g. `toBeInTheDocument`) install
// `@testing-library/jest-dom` and import it here.

import '@testing-library/jest-dom';;

export {};
